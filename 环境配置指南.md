# 环境配置指南

在运行本项目之前，您需要配置以下环境：

## 一、必需软件环境

### 1. Python 环境
- **版本要求**：Python 3.8 或更高版本
- **安装方式**：
  - Windows: 从 [Python官网](https://www.python.org/downloads/) 下载安装
  - 安装时勾选 "Add Python to PATH"
- **验证安装**：
  ```bash
  python --version
  ```

### 2. Node.js 环境
- **版本要求**：Node.js 16.x 或更高版本（推荐使用 LTS 版本）
- **安装方式**：
  - 从 [Node.js官网](https://nodejs.org/) 下载安装
  - 或使用包管理器安装
- **验证安装**：
  ```bash
  node --version
  npm --version
  ```

### 3. MySQL 数据库
- **版本要求**：MySQL 5.7 或更高版本（推荐 MySQL 8.0+）
- **安装方式**：
  - 从 [MySQL官网](https://dev.mysql.com/downloads/mysql/) 下载安装
  - 或使用 XAMPP/WAMP 等集成环境
- **验证安装**：
  ```bash
  mysql --version
  ```

## 二、数据库配置

### 1. 创建数据库
在 MySQL 中创建项目所需的数据库：

```sql
CREATE DATABASE university_homework_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. 配置数据库连接
编辑 `backend/app/config.py` 文件，修改数据库连接信息：

```python
SQLALCHEMY_DATABASE_URI = 'mysql+pymysql://用户名:密码@127.0.0.1:3306/university_homework_system'
```

**默认配置**：
- 用户名：`root`
- 密码：`rootroot`
- 主机：`127.0.0.1`
- 端口：`3306`
- 数据库名：`university_homework_system`

**或者使用环境变量**：
```bash
# Windows PowerShell
$env:DATABASE_URL="mysql+pymysql://用户名:密码@127.0.0.1:3306/university_homework_system"

# Windows CMD
set DATABASE_URL=mysql+pymysql://用户名:密码@127.0.0.1:3306/university_homework_system
```

## 三、后端环境配置

### 1. 创建 Python 虚拟环境
在项目根目录（`Taskly-master/Taskly-master/`）下执行：

**Windows:**
```bash
python -m venv venv
```

### 2. 激活虚拟环境

**Windows PowerShell:**
```bash
.\venv\Scripts\Activate.ps1
```

**Windows CMD:**
```bash
venv\Scripts\activate.bat
```

如果 PowerShell 执行策略限制，需要先运行：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 3. 安装 Python 依赖
在激活虚拟环境后，安装项目依赖：

```bash
# 使用清华镜像源（推荐，速度更快）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或使用默认源
pip install -r requirements.txt
```

### 4. 初始化数据库表
项目使用 Flask-Migrate 进行数据库迁移，首次运行需要初始化：

```bash
# 进入后端目录
cd backend

# 初始化迁移（如果 migrations 目录已存在，可跳过）
flask db init

# 创建迁移文件
flask db migrate -m "Initial migration"

# 应用迁移，创建数据库表
flask db upgrade
```

## 四、前端环境配置

### 1. 安装 Node.js 依赖
进入前端目录并安装依赖：

```bash
cd frontend
npm install
```

如果安装速度慢，可以使用国内镜像：

```bash
npm install --registry=https://registry.npmmirror.com
```

## 五、环境变量配置（可选）

### JWT 密钥配置
如果需要自定义 JWT 密钥，可以设置环境变量：

**Windows PowerShell:**
```bash
$env:JWT_SECRET_KEY="your-secret-key-here"
```

**Windows CMD:**
```bash
set JWT_SECRET_KEY=your-secret-key-here
```

如果不设置，将使用默认值 `'super-secret'`（仅用于开发环境，生产环境请务必修改）。

## 六、验证配置

### 1. 检查后端配置
```bash
cd backend
python run.py
```
如果看到 Flask 应用启动信息，说明后端配置成功。

### 2. 检查前端配置
```bash
cd frontend
npm run dev
```
如果看到 Vite 开发服务器启动信息，说明前端配置成功。

## 七、常见问题

### 1. MySQL 连接失败
- 检查 MySQL 服务是否启动
- 确认数据库用户名和密码正确
- 确认数据库已创建
- 检查防火墙设置

### 2. Python 包安装失败
- 尝试使用清华镜像源
- 升级 pip: `python -m pip install --upgrade pip`
- 检查网络连接

### 3. Node.js 依赖安装失败
- 清除缓存: `npm cache clean --force`
- 删除 `node_modules` 和 `package-lock.json` 后重新安装
- 使用国内镜像源

### 4. 数据库迁移失败
- 确保数据库已创建
- 检查数据库连接配置
- 确保有足够的数据库权限

## 八、启动项目

### 启动后端
```bash
cd backend
python run.py
```
后端默认运行在 `http://127.0.0.1:5000`

### 启动前端
```bash
cd frontend
npm run dev
```
前端默认运行在 `http://localhost:5173`（Vite 默认端口）

---

**注意**：首次运行前，请确保已完成所有环境配置步骤，特别是数据库的创建和迁移。

